# 酒店素材工坊｜项目对话与修改记录

> 整理日期：2026-09-23  
> 当前交付：智能 EDL 双模式自动成片流程  
> 原则：保留旧流程、智能流程可回退、渲染结果可验片、本机配置不进入仓库

## 1. 最终目标

将酒店素材自动成片从“视频生成模型主导”调整为“旁白时间轴和本地素材主导”：

```text
文案
  ↓
TTS 生成旁白
  ↓
ASR / 估算旁白时间轴
  ↓
素材缩略图 + 镜头标签 + 旁白段落
  ↓
视觉 / 剪辑模型生成 EDL
  ↓
FFmpeg 拼接、字幕烧录、旁白与背景音乐混音
  ↓
首帧、切点、音轨自动验片
  ↓
MP4 导出
```

智能流程不再调用 Seedance；旧版流程继续保留为“快速本地”模式。

## 2. 对话决策记录

### 2.1 从 Seedance 转向 TTS + 本地剪辑

- 用户提出音色生成直接使用 TTS，不再依赖 Seedance 的音频能力。
- 结论：旁白由 Turing TTS 生成，画面使用用户上传的原始图片 / 视频，后期本地剪辑。
- Seedance 不从项目删除，作为旧版快速本地流程和历史备份保留。

### 2.2 引入 Video Use 风格的智能 EDL

- 用户希望 LLM 根据素材内容和时间戳判断切点。
- 用户希望文案与画面自动匹配，并在渲染后检查结果。
- 结论：新增智能 EDL 模式，模型只决定素材选择、切点和原因；旁白文案不允许被剪辑模型改写。

### 2.3 TTS 是否必要

- 确认 TTS 是智能流程的核心，而不是可选装饰：它提供实际旁白时长和词级 / 段落级时间轴。
- ASR 可选；ASR 不可用时使用按文案长度估算的旁白时间轴，并在状态中明确标记“估算时间轴”。

### 2.4 保留旧流程并备份

- 原 Seedance / Canvas 代码没有删除。
- 历史版本备份位置：`backups/auto-edit-seedance-20260923/`
- 页面新增两个剪辑引擎：
  - `智能 EDL`：默认选中；TTS → ASR → EDL → FFmpeg。
  - `快速本地`：旧版 Canvas / Seedance 流程。

### 2.5 已处理的问题

1. **TTS 音色报错**：服务端支持模型实际接受的 `alloy / echo / fable / onyx / nova / shimmer` 等音色映射，不再把不支持的 `tts-1` 当作 voice 传入。
2. **API 反复输入**：API Key 使用 macOS Keychain 持久化；本机运行状态不再提交到 Git。
3. **视觉模型 ID 强制校验**：视觉 / 剪辑模型可配置；留空或不可用时回退本地 EDL，不阻塞基础流程。
4. **预览与导出字幕不一致**：预览、Canvas 和 FFmpeg ASS 使用同一套字幕字段和样式规则。
5. **字幕样式问题**：完整文案作为单一 headline 输出；隐藏旧的下方绿色字幕；中文最多两行；文字白色填充；描边变细；同一视频内统一字号。
6. **字幕与画面错配**：旁白段落、画面段、字幕段共用 EDL 时间轴；视觉模型只选镜头，不重写文案。
7. **配音节奏异常**：优先使用 ASR 词级时间戳；ASR 失败时使用明确的估算时间轴，并按照旁白分段生成画面。
8. **背景音乐丢失**：智能导出将背景音乐作为独立音频输入，用 FFmpeg 与旁白按低音量混音；响应头会标记 `X-Hotel-BGM: mixed`。
9. **文案病句**：AI 文案生成改为更完整的句式模板，支持主题、亮点、受众和优惠活动组合；生成后仍可手动编辑。
10. **上传素材无法删除**：素材卡片支持删除，并释放本地 Object URL，删除后要求重新生成相关结果。
11. **`String(...).toFixed` 导出错误**：修复字号缩放值的数值转换，避免智能成片在前端直接失败。
12. **字幕动效单调**：按字幕段和文案内容生成可复现的稳定随机动效，包括淡入淡出、打印机、左滑、右滑、上滑；预览和导出共用动效选择。

## 3. 最终功能架构

### 3.1 前端

- `demo/index.html`：双引擎入口、TTS 音色、字幕、背景音乐和导出界面。
- `demo/ai-video-runtime.js`：
  - TTS 请求和音频时长读取；
  - ASR 时间轴请求；
  - 素材缩略图 / 元数据清单；
  - 智能 EDL 请求和本地回退；
  - Canvas 旧流程；
  - 智能 MP4 导出；
  - 预览、字幕动效和时间线编辑。
- `demo/styles.css`：双引擎 UI、预览字幕和字幕样式。

### 3.2 服务端接口

| 接口 | 作用 |
| --- | --- |
| `POST /api/generate-audio` | 使用 Turing TTS 生成旁白 |
| `POST /api/align-audio` | 调用 ASR，获取 word timestamps；不可用时前端回退估算时间轴 |
| `POST /api/analyze-asset-tags` | 根据素材缩略图推荐客房、泳池、餐饮、大堂、周边等镜头标签 |
| `POST /api/analyze-edit` | 将缩略图、标签和旁白段落交给视觉 / 剪辑模型生成 EDL |
| `POST /api/render-smart-edit` | FFmpeg 拼接、字幕烧录、音频混音和自动验片 |
| `POST /api/generate-video` | 仅由快速本地旧流程调用 Seedance |

### 3.3 EDL 约束

每个 `range` 至少包含：

- `assetIndex`
- `sourceStart` / `sourceEnd`
- `voiceStart` / `voiceEnd`
- `captionStart` / `captionEnd`
- `text`
- `beat`
- `reason`
- `motion`

智能剪辑模型必须返回与旁白段落数量一致的 range；不满足时自动使用本地 EDL，并在页面显示 `本地回退 EDL`。

## 4. 字幕最终规则

- 完整文案作为主字幕层输出。
- 每个字幕段最多两行中文。
- 全部文字白色填充，不再用蓝色 / 暖色放大重点词。
- 同一个视频内使用共享字号，根据所有字幕段最长行统一缩放。
- 黑色描边使用细描边，避免覆盖画面内容。
- 横线与文字距离缩短。
- 预览和 FFmpeg ASS 使用同样的换行、字号和动效选择。

## 5. 音频最终规则

- 旁白音量沿用页面设置。
- 背景音乐默认低音量混入，当前智能导出使用 `0.18` 增益。
- 原始上传视频音频不泄漏到智能流程，旁白由 TTS 统一提供。
- 背景音乐关闭时不创建音乐输入，也不伪造音乐已混入。

## 6. 验证记录

### 静态检查

已通过：

```text
node --check demo/ai-video-runtime.js
node --check demo/server.mjs
```

### 智能 MP4 渲染检查

使用本地素材、旁白和背景音乐执行过真实的 `POST /api/render-smart-edit`，返回：

```text
HTTP/1.1 200 OK
X-Hotel-Render: video-use-edl
X-Hotel-BGM: mixed
X-Hotel-Verify: decode-pass
```

检查过：

- 首帧可以解码；
- 多个字幕切点可以解码；
- 音频流可以解码；
- 背景音乐确实进入混音链路；
- FFmpeg 输出为 MP4。

### 页面检查

- 新页面默认显示“智能 EDL”；
- 可以切换到“快速本地”；
- 切换后状态提示与实际分支一致；
- API 配置状态显示为本机持久配置；
- 旧版 Seedance 代码和备份仍在仓库中。

## 7. 发布与安全说明

- `demo/.runtime-settings.json` 仅保留在本机，用于保存非敏感运行设置；已加入 `.gitignore`，不会发布。
- API Key 继续保存在 macOS Keychain，不写入项目源码、浏览器 `localStorage` 或 Git。
- `node_modules/`、临时目录和本地安装包不进入仓库。
- 历史 Seedance 流程保留在 `backups/auto-edit-seedance-20260923/`，便于回看和恢复。

## 8. 当前发布结论

最终版本以“智能 EDL 默认 + 快速本地兼容”的双模式为准。智能模式负责稳定的 TTS 时间轴、素材匹配、字幕一致性、FFmpeg MP4 和自动验片；快速本地模式负责保留旧版 Seedance / Canvas 体验。两条链路互不覆盖，后续可以分别迭代。
